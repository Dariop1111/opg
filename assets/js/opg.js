new Splide( '.slider1-wrap',{
    type:"slide",
    rewind: true,
    autoplay: true,
    interval: 4000,
} ).mount();
new Splide( '.slider2-wrap',{
    type:"loop",
    perPage:3,
    autoplay: true,
    interval: 4000,
} ).mount();


